import { DeselectableDirective } from './deselectable.directive';

describe('DeselectableDirective', () => {
  it('should create an instance', () => {
    const directive = new DeselectableDirective();
    expect(directive).toBeTruthy();
  });
});
